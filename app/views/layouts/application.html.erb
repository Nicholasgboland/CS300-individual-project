<!DOCTYPE html>
<html>
  <head>
    <title>CS300IndividualProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  <body>

    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
      <div class="container-fluid">
      <%= link_to "Portfolio Projects", projects_path, :class => "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav me-auto mb-2 mb-md-0">
            <li class="nav-item">
              <%= link_to "Home", projects_path, :class => "nav-link active" %> 
            </li>
            <li class="nav-item">
            <%- if user_signed_in? %>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, 'data-turbo': false, :class => "nav-link d-flex align-items-right border-0", :style => "background-color:transparent" %>
            <% else %>
              <%= button_to "Sign In / Sign Up", new_user_session_path, 'data-turbo': false, :class => "nav-link d-flex align-items-right border-0", :style => "background-color:transparent" %>
            <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>

  </body>

</html>
